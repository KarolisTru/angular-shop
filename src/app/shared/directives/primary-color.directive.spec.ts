import { PrimaryColorDirective } from './primary-color.directive';

describe('PrimaryColorDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimaryColorDirective();
    expect(directive).toBeTruthy();
  });
});
